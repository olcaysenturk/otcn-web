export interface Country {
    id: number;
    code: string;
    name: string;
    dialCode: string;
}

export interface City {
    id: number;
    name: string;
}

export interface District {
    id: number;
    name: string;
}
